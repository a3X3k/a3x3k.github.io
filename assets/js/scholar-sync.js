// Optional Scholar data bridge.
// This static site uses local publication data by default.
// To enable live-like updates, replace assets/data/scholar-profile.json with exported metrics.
window.SCHOLAR_SYNC_READY = true;
